import { Markdown, Meta } from '@storybook/addon-docs/blocks';
import principles from '../../../PRINCIPLES.md?raw';
import { Credit } from './Credit';
import { Masthead } from './Masthead';

<Meta title="Introduction" />

<Masthead />

<Markdown>{principles}</Markdown>

## Packages

| Package | What it is |
| --- | --- |
| `@pearpages/pulp-tokens` | DTCG JSON source, `tokens.css` and `tokens.json` out |
| `@pearpages/pulp-css` | layer order, reset, base styles; framework-agnostic |
| `@pearpages/pulp-react` | components, one entry per component |
| `@pearpages/pulp-icons` | stroke icons as React components, generated from SVG |

## Find your way

Components are grouped by kind, what you reach for, never by when they were built:
Typography, Layout, Actions, Forms, Navigation, Overlays, Feedback, Data, Utilities. Each
component has a Docs page (usage, accessibility, do and don't, props, stories) rendered from
its JSDoc through the component manifest, and the **Status** page lists all of them by
category with their maturity. **Decisions** records why the system is shaped as it is.

## Use it

Load the brand typefaces yourself (`@fontsource-variable/archivo`, `instrument-sans`,
`geist-mono` for pulp; `nunito`, `inter` for bitepals). Tokens name the families and
fall back to system fonts; they ship no font files.

```css
/* once per app */
@import "@pearpages/pulp-css";
@import "@pearpages/pulp-react/styles.css";
```

```tsx
import { Button } from '@pearpages/pulp-react/button';

<html data-brand="bitepals" data-scheme="dark">
  <Button variant="secondary">Save</Button>
</html>
```

<Credit />
