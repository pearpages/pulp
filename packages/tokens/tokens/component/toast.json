{
  "$description": "Toast component tokens: the region and its stack; each toast is an Alert.",
  "toast": {
    "width": {
      "$type": "dimension",
      "$value": "{size.toast-width}"
    },
    "offset": {
      "$type": "dimension",
      "$description": "From the inline edge of the viewport. The centred placements ignore it.",
      "$value": "{space.5}"
    },
    "offset-block": {
      "$type": "dimension",
      "$description": "From the top or the bottom edge, on top of the safe-area inset. Separate from the inline offset so an app with a bottom navigation can lift the stack above it without pushing it sideways.",
      "$value": "{space.5}"
    },
    "gap": {
      "$type": "dimension",
      "$value": "{space.3}"
    },
    "layer": {
      "$type": "number",
      "$value": "{layer.toast}"
    },
    "shadow": {
      "$type": "shadow",
      "$value": "{shadow.overlay}"
    },
    "radius": {
      "$type": "dimension",
      "$value": "{radius.surface}"
    },
    "motion": {
      "duration": {
        "$type": "duration",
        "$value": "{motion.duration.base}"
      }
    }
  }
}
