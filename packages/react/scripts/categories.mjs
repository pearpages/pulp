/**
 * The component categories, in sidebar order. A category is a kind, what a
 * consumer reaches for, never a build tier. Declared once here; the manifest
 * carries the list and each component's `@category`, and the Storybook, the
 * status page and the scaffold read it from there.
 */
export const CATEGORIES = ['Typography', 'Layout', 'Actions', 'Forms', 'Navigation', 'Overlays', 'Feedback', 'Data', 'Utilities'];
