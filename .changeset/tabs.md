---
"@pearpages/pulp-react": minor
"@pearpages/pulp-tokens": minor
---

Add `Tabs` with `Tabs.List`, `Tabs.Tab` and `Tabs.Panel` following the WAI-ARIA
Tabs pattern: one tab stop, arrow keys with wraparound that skip disabled tabs,
Home and End, automatic or manual activation, horizontal or vertical, controlled
or uncontrolled. `--tabs-*` tokens and a reusable roving-focus hook.
