---
'@pearpages/pulp-tokens': patch
---

The bitepals brand's `font.weight.medium` and `font.weight.semibold` are 500 and 600, the weights the product has always used; they were 600 and 700, a step too heavy, and bitepals overrode both. The pulp brand is unchanged.
