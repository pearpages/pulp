---
"@pearpages/pulp-react": minor
"@pearpages/pulp-tokens": minor
---

Add `Text` and `Heading`: the type scale as components. `Text` has size, weight,
tone, family, alignment and truncation; `Heading` separates outline level from
visual size. `--text-*` and `--heading-*` tokens.
