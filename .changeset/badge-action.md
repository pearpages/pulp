---
'@pearpages/pulp-react': minor
'@pearpages/pulp-tokens': minor
---

Badge: `tone="action"`, the brand's colour, in the three variants. For a marker that is not a status, such as the unread dot or count on a navigation item; it reads `--badge-action-*`, which point at the action tokens whose contrast is already tested.
