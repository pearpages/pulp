---
"@pearpages/pulp-react": minor
"@pearpages/pulp-tokens": minor
---

Add `TextField`: label, description and error wired through `aria-describedby`,
invalid state via `aria-invalid` and `data-invalid`, sizes, controlled and
uncontrolled. New semantic token `color.status.error-text` for error text that
meets AA on every surface; `--text-field-*` component tokens.
