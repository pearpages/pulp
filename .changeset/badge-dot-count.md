---
'@pearpages/pulp-react': minor
'@pearpages/pulp-tokens': minor
---

Badge: `variant="dot"`, a presence mark with no visible text whose `label` is rendered for assistive technology, and `count` with `max` (default 99, shown as "99+"; zero or less renders nothing). With a `count`, `label` says what is counted ("3 unread"). Token: `--badge-dot-size`.
