---
'@pearpages/pulp-tokens': patch
'@pearpages/pulp-css': patch
'@pearpages/pulp-icons': patch
'@pearpages/pulp-react': patch
---

Package metadata: `keywords`, `homepage` (the docs site) and `bugs` (GitHub issues) on every package, so npm search finds them and each npm page links back to the docs.
