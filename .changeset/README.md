# Changesets

Run `pnpm changeset` after a change that consumers can notice, pick the
packages and the bump, and describe it for the changelog. `pnpm version-packages`
turns pending changesets into versions and changelog entries; a `vX.Y.Z` tag on
`main` then publishes.
