## What and why

## Checked

- [ ] `pnpm verify` passes locally
- [ ] A changeset, if a published package changed (`pnpm changeset`)
- [ ] Visual change? `visual-update.yml` run on this branch and the image diff reviewed
